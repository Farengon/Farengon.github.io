# 2026-08-06 工作日志

## 批量生成 AP CSA 章节文档（47 个）
- 从 ima 知识库 "AP CSA"（kb id: 7490268156674451）获取课程大纲（CED PDF，2025 Fall 版，4-Unit 结构）与 APClassroom 题库（含答案解析 PDF）。
- 章节结构（新 CED）：Unit1: 1.1-1.15（Using Objects and Methods）；Unit2: 2.1-2.12（Selection and Iteration）；Unit3: 3.1-3.9（Class Creation）；Unit4: 4.1-4.17（Data Collections）。
- 以 csa/unit1/topic1-1.md ~ topic1-6.md 为模板，生成全部剩余章节：
  - unit1: topic1-7 ~ topic1-15
  - unit2: topic2-1 ~ topic2-12
  - unit3: topic3-1 ~ topic3-9
  - unit4: topic4-1 ~ topic4-17
- 每个文档格式：YAML frontmatter (layout: default, title, parent: Unit X, nav_order) + 中文知识点分节（X.Y.A / X.Y.B）+ 例题（Source 引用 AP Classroom 题库真实题目，含选项与"点击查看解答"折叠块，详细中文解析）+ 考点总结表格。
- 同步更新 csa/unit2/index.md 大纲为新结构（原为旧版 9 主题，现为 2.1-2.12）。
- 生成报告：D:\ap\Farengon.github.io\生成报告.txt（列出全部生成文件）。

## 经验
- ima 知识库章节列表接口 get_knowledge_list limit 最大 50；文件夹用 folder_id 参数遍历。
- 题库 PDF 带答案版（"- Answer"后缀）含完整解析，是生成例题的最佳来源。
- CED PDF 本地副本（csa/ap-computer-science-a-course-and-exam-description.pdf）为加密 PDF，需 pypdf + cryptography 才能读取，用 pypdf 提取 topic 列表页面（page 27/33-34/64-65/84-85/102-103）。

## 修复 GitHub Pages 构建错误（Liquid 语法）
- 报错原因：unit4 中 topic4-11/4-12/4-13 的 Java 二维数组初始化 `{{9, 8, 7}, ...}` 被 Jekyll Liquid 当作模板变量解析，导致 build 失败。
- 修复：所有含 `{{ }}` 的代码块外包 `{% raw %} ... {% endraw %}`；行内代码/选项文本用行内 raw 标签包裹。三个文件 raw/endraw 标签均已配对。
- 附带修复：unit2/index.md 的 title 改为 "Unit 2"（原含全角冒号，与子页面 `parent: Unit 2` 不匹配，just-the-docs 导航会失效）。
- 经验：Jekyll 中 Markdown 内容里的 `{{`、`{%` 必须用 raw 标签转义，Java/JS 代码块含花括号时尤其要注意；子页面 parent 必须与父页面 title 完全一致。
